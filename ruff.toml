line-length = 100
target-version = "py310"

extend-exclude = [
    "benchmarks",
    "scratch_simple_repo",
    "scratch_test",
    "scratch_test_conf",
]

[lint]
select = ["E", "W", "F", "I"]
ignore = ["E501"]
